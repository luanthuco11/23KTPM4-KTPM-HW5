# Evidence checklist

Store only real, attributable evidence here.

- `hardware/`: dxdiag screenshot and hardware specification table.
- `load/`: JMeter and Task Manager in the same frame.
- `stress/`: JMeter and Task Manager in the same frame.
- `spike/`: JMeter and Task Manager in the same frame.
- `soak/`: sustained-load monitoring evidence.
- `video-link.txt`: unlisted YouTube link after the student uploads the narrated recording.

